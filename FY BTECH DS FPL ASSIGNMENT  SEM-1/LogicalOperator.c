//C program to illustrate logical operator 
#include<stdio.h>
int main()
{
    int a=15, b=3;
    //using operators and printing results
    // 1 is true and 0 is false
      printf("a && b : %d\n", a && b);
    printf("a || b : %d\n", a || b);
    printf("!a: %d\n", !a);

    return 0;
}